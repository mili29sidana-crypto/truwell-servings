kind = "web"
previewPath = "/"
title = "Truwell"
version = "1.0.0"
id = "artifacts/truwell-site"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19809

[services.development]
run = "pnpm --filter @workspace/truwell-site run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/truwell-site", "run", "build" ]
publicDir = "artifacts/truwell-site/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19809"
BASE_PATH = "/"
